# claude-para-llm-wiki — Python dependencies
# Install: pip install -r requirements.txt
# Optional deps marked with [platform]

# --- Security: Prompt injection scanning ---
llm-guard>=0.3.0

# --- Content extraction ---
trafilatura>=1.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Audio transcription (Apple Silicon Mac) ---
# Install separately on Apple Silicon: pip install mlx-whisper
# mlx-whisper>=0.3.0  # uncomment on Apple Silicon

# --- Audio transcription (universal fallback: Mac Intel, Linux, Windows) ---
faster-whisper>=1.0.0

# --- Shared transcription utility ---
torch>=2.0.0
